\ProvidesPackage{eyamrog}

\usepackage{import}
%\usepackage{graphicx} % Required for inserting images
\usepackage{tikz} % Enables TikZ graphic elements package
    \tikzset{>=latex}
    \usetikzlibrary{shapes.geometric} % Enables the use of predefined shapes
    \usetikzlibrary{matrix} % Enables multiple nodes inside a node
    \usetikzlibrary{decorations.pathreplacing}
    \usetikzlibrary{decorations.markings}
\usepackage{pgfplots}
    \pgfplotsset{compat=1.8}
\usepackage{geometry} % Defines the page setup
    \geometry{
    a4paper,
    %landscape,
    total={160mm, 247mm},
    %total={247mm, 160mm},
    left=30mm,
    top=30mm}
\usepackage{pdflscape} % Allows landscape pages
\usepackage{parskip} % Enables paragraph spacing with default options
%\usepackage[indent=2em]{parskip} % Enables paragraph spacing with indentation
\usepackage{setspace} % Enables line spacing
    \onehalfspacing % Sets the line spacing of the document as 1.5
\usepackage{changepage} % Introduces the environment 'adjustwidth' for paragraphs with specific indentation
\usepackage{epigraph}
\usepackage{titling} % Allows multiple instances of 'title'
%\usepackage[center]{titlesec} % Use it to center the titles of the document
\setcounter{secnumdepth}{3} % Increases the section numbering depth
%\usepackage{lineno} % Use it to add line numbering
%\linenumbers % Turns on line numbering
%\usepackage{showframe} % Use it to check the document's page setup
\usepackage{lipsum} % Use it to generate text to help check the document's page setup
\usepackage{blindtext} % Use it to generate text to help check the document's page setup
\usepackage{url} % Allows URLs to be clickable
\usepackage{hyperref} % Allows the use of hyperlinks (due to a known conflict, make sure it is loaded before 'abntex2cite')
\usepackage[natbibapa]{apacite} % Use it to enable APA style
%\usepackage[alf]{abntex2cite} % Use it to enable ABNT style
\usepackage[utf8]{inputenc} % Sets the document encoding to UTF8
\renewcommand{\rmdefault}{\sfdefault} % Changes to sans serif typeface
\usepackage[brazilian, english]{babel} % Sets multilingual support to 'brazilian' and 'english'
\usepackage{caption} % Enables better management of captions
\usepackage{float} % Enables better management of floating objects
\usepackage{multirow} % Enables the combination of rows and columns in tables
%\usepackage{emoji} % Requires that the compiler is changed to LuaLaTeX
\usepackage{longtable} % Enables multi-page tables
\usepackage{booktabs} % Enhances the quality of tables, providing extra commands as well as behind-the-scenes optimisation
\usepackage{tipa} % Enables phonetic symbols (IPA)
\usepackage{listings} % Enables computer code listing
\usepackage[T1]{fontenc} % Switch the font encoding to T1, which provides support for the '\dh' command and other special characters
\renewcommand{\thesection}{\arabic{section}} % When using the 'report' document class without chapters, this setting removes the leading '0' on the numbering of sections
\usepackage[export]{adjustbox} % Enables drawing a frame around the figures with the command '\fbox'
\usepackage{multicol} % Enables the creation of columns of text
\usepackage{xcolor} % Enables the use of colours
\usepackage{soul} % Enables text highlighting with the command '\hl'
\usepackage[normalem]{ulem} % Enables striking through pieces of text with the command '\sout'
\usepackage{cancel} % Enables crossing out pieces of text with the commands '\cancel', '\bcancel' and '\xcancel' in both math and text modes

% Define colours for highlighting text
% https://www.w3schools.com/colors/colors_picker.asp
% https://colorhunt.co/
\definecolor{lightyellow}{RGB}{255, 255, 128}
\definecolor{lightgreen}{RGB}{204, 255, 204}
\definecolor{lightorange}{RGB}{255, 204, 128}
\definecolor{lightmagenta}{RGB}{255, 128, 223}
\definecolor{lightblue}{RGB}{128, 179, 255}
\definecolor{lightpurple}{RGB}{191, 128, 255}

% Define new commands for highlighting text with the defined colours
\newcommand{\hlyellow}[1]{\sethlcolor{lightyellow}\hl{#1}}
\newcommand{\hlgreen}[1]{\sethlcolor{lightgreen}\hl{#1}}
\newcommand{\hlorange}[1]{\sethlcolor{lightorange}\hl{#1}}
\newcommand{\hlmagenta}[1]{\sethlcolor{lightmagenta}\hl{#1}}
\newcommand{\hlblue}[1]{\sethlcolor{lightblue}\hl{#1}}
\newcommand{\hlpurple}[1]{\sethlcolor{lightpurple}\hl{#1}}

% Define the 'example' environment
% Usage:
%\begin{example}{Dimension 1 positive}{eg:dim1-1}
%    \textit{Despite \dots}
%\end{example}

\newcounter{examplecounter}
\newenvironment{example}[2]{
    \refstepcounter{examplecounter}
    \vspace{1em}
    \noindent\hspace*{10mm} % Add horizontal space here
    \minipage{\dimexpr\textwidth-10mm\relax} % Reduce the width of the minipage
    Example \theexamplecounter: #1
    \label{#2}
    \\
}{
    \vspace{.75em}
    \endminipage
    \par
}
